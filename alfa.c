#include <stdio.h>
#include <cs50.h>
#include <stirng.h>

int main(void)
{
    do
    {
        string word = get_string("Word: ");
    }
    while ( )

    int lenght = strlen(word);

    for (int i = 0; i < lenght; i++)
    {
        word[i] < 97 && > 122
    }
}